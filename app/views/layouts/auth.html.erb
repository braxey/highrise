<!DOCTYPE html>
<html>
  <head>
    <title>
      <% if content_for?(:title) %>
        <%= content_for(:title) %> - Phoenix
      <% else %>
        Phoenix
      <% end %>
    </title>

    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="flex min-h-screen flex-col bg-[#FDFDFC]">
      <main class="flex flex-1 flex-col items-center justify-center gap-8 p-6 lg:p-8">
        <div class="w-full max-w-md rounded-lg bg-white p-8 shadow-lg">
          <div class="flex flex-col items-center gap-6">
            <div class="space-y-3 text-center">
              <h1 class="text-2xl font-semibold text-neutral-900"><%= content_for(:card_title) || content_for(:title) %></h1>
              <% if content_for?(:description) %>
              <p class="text-sm text-neutral-700"><%= content_for :description %></p>
              <% end %>
            </div>

            <% if flash[:notice] %>
              <p class="text-sm text-emerald-600"><%= flash[:notice] %></p>
            <% end %>

            <div class="w-full">
              <%= yield %>
            </div>
          </div>
        </div>
      </main>
    </div>
  </body>
</html>
