<%
  sidebar_nav_items = [
    {
      title: 'Organization',
      href: organization_path(organization),
    },
    {
      title: 'Edit',
      href: edit_organization_path(organization),
    },
    {
      title: 'Members',
      href: organization_organization_memberships_path(@organization),
    },
    {
      title: 'Invite Member',
      href: new_organization_organization_invitation_path(@organization),
    }
  ]
%>

<% content_for(:section_header, "#{organization.name}") %>
<% content_for(:section_subheader, "Manage members and settings for #{organization.name}") %>

<%= render "shared/shells/side_nav_shell", sidebar_nav_items: sidebar_nav_items do %>
  <%= yield %>
<% end %>
