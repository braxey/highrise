<!-- app/views/roles/_role.html.erb -->
<div class="flex flex-col justify-between items-start gap-2 rounded-md border p-4 shadow-md bg-white hover:shadow-lg transition-shadow duration-200">
  <h3 class="text-lg font-semibold text-gray-800"><%= role.name %></h3>
  <p class="text-sm text-gray-600">Scope: <%= role.scope.humanize %></p>
  <p>
    <%= link_to "Edit", edit_role_path(role), class: "text-emerald-600 hover:underline" %>
  </p>
</div>